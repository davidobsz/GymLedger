@model GymLedger.Views.Areas.Profile.DeleteUserView

<div class="modal fade" id="modalModal" tabindex="-1" aria-labelledby="modalModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content shadow-lg rounded-4 border-0">

            <!-- Modal Header -->
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title fw-bold" id="modalModalLabel">Delete Account</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <!-- Modal Body -->
            <div class="modal-body">
                <p><b class="text-danger">Warning:</b> deleting your account is permanent with no way of restoring it.</p>
                @using (Html.BeginForm("DeleteAccount", "Profile", FormMethod.Post, new { @class = "form-horizontal", role = "form", id = "deleteUserForm" }))
                {

                    <!-- Current Password -->
                    <div class="mb-3">
                        @Html.LabelFor(m => m.CurrentPassword, new { @class = "form-label fw-semibold" })
                        @Html.PasswordFor(m => m.CurrentPassword, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.CurrentPassword, "", new { @class = "text-danger small" })
                    </div>

                    <!-- Submit Button -->
                    <div class="text-end mt-3">
                        <button type="submit" class="btn btn-success px-4">
                            <i class="fa-solid fa-check"></i> Submit
                        </button>
                    </div>
                }
            </div>

        </div>
    </div>
</div>
